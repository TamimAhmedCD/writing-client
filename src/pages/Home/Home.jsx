import { useEffect } from "react";

const Home = () => {
      useEffect(() => {
        document.title = "Home | Writing";
      }, [])
    return (
        <div>
            
        </div>
    );
};

export default Home;